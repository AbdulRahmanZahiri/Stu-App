# Claude Code Instructions

Use `AGENTS.md` as the shared project rules. Before editing, read
`docs/AI_HANDOFF.md`, inspect `git status`, and record the files you intend to
change. Do not overwrite concurrent Codex work; use a separate Git worktree and
branch when both agents are active.
